# Core app
FRONTEND_BASE_URL=https://your-frontend.vercel.app
YOUTUBE_BACKEND_REDIRECT_URI=https://your-backend.onrender.com/auth/youtube/callback

# Database
# For local SQLite dev, leave DATABASE_URL empty.
# For production (Supabase Postgres), set DATABASE_URL from Supabase -> Connect -> Transaction pooler URI.
DATABASE_URL=
AUTH_DB_PATH=app.db

TRIAL_DAYS=60

# Billing (optional)
# Use a live sk_live_ key for real card checkout. sk_test_ keys keep Stripe in test mode.
STRIPE_SECRET_KEY=

# Email (optional, for password reset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Limits
MAX_UPLOAD_MB=500
